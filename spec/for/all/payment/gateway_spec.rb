RSpec.describe For::All::Payment::Gateway do
  it "has a version number" do
    expect(For::All::Payment::Gateway::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
